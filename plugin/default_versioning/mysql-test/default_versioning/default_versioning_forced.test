SHOW VARIABLES LIKE 'default_versioning_%';

SET GLOBAL default_versioning_forced=ON;
SET GLOBAL default_versioning_hidden=OFF;
SHOW VARIABLES LIKE 'default_versioning_%';

CREATE TABLE t(x INT UNSIGNED NOT NULL);
SHOW CREATE TABLE t;

# It is required that the state before test is preserved including variables.
DROP TABLE t;
SET GLOBAL default_versioning_forced=ON;
SET GLOBAL default_versioning_hidden=ON;
